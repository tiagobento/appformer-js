{
  "name": "appformer-js",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "init": "lerna bootstrap",
    "build": "lerna run build --stream",
    "build:colorful": "lerna exec -- yarn run build"
  },
  "devDependencies": {
    "lerna": "3.4.0"
  }
}
