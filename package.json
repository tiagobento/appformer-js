{
  "name": "appformer-js",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "init": "npx lerna bootstrap",
    "build": "npx lerna run build"
  },
  "devDependencies": {
    "lerna": "3.13.1"
  }
}
